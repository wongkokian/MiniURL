<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <title>MiniURL - Minimize your long URLs!</title>
</head>
<body style="background-color:#181818">
    <div class="container">
        <h1 style="color:#F2F2F2; font-family: 'Didot'; font-size:10vw; text-align: center; padding-top: 20px;">MiniURL</h1>
        <form action="/shortUrls" method="POST" class="my-4 form-inline">
            <div style="text-align: center">
                <input required placeholder="Insert your long URL here! :)" type="url" name="fullUrl" id="fullUrl" class="form-control-lg" style="width: 70%; font-family: 'Didot'">
            </div>
            <div style="text-align: center; padding: 30px">
                <button class="btn" style="background-color: #5CB8E4; font-size: x-large; font-family: 'Didot'" type="submit">Minimize!</button>
            </div>
        </form>

        <div class="row justify-content-center">
            <table class="table table-striped table-hover table-responsive" style="background-color:#3F4E4F; color:#F2F2F2; width: 70%; font-family: 'Didot'; border-radius: 10px; border-style: hidden;">
                <thead>
                    <tr>
                        <th>Long URLs</th>
                        <th>MiniURLs</th>
                    </tr>
                </thead>
                <tbody>
                    <% shortUrls.forEach(shortUrl => { %>
                        <tr>
                            <td><a href="<%= shortUrl.full %>" style="color: #F2F2F2; text-decoration: none"><%= shortUrl.full %></a></td>
                            <td><a href="<%= shortUrl.short %>" style="color: #F2F2F2; text-decoration: none">http://localhost:5000/<%= shortUrl.short %></a></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>